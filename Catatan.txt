"#" --> hide konten (untuk password)
PAHAMI "SET" & "GET"
	.include = harus memuat 

.include = data yg diinput harus memuat
Contoh: 
set validateEmail(email){
        if(email.includes('@') && email.includes('.')){
            this.email = email
        }else{
            console.log('Email Not Valid');
        }
    }


"SETTER"/"SET" Untuk men'set' value properti object --> set namaProperti(value) --> TIDAK BISA DALAM "IF" --> TIDAK BISA DALAM "IF" & DALAM VARIABLE
Contoh
set validateProgramSelected(program){
        const validPrograms = ['JCWD', 'JCDS', 'JCUI/UX', 'JCDM', 'JCVD']

        if(validPrograms.includes(program.toUpperCase())){
            this.programSelected = program.toUpperCase()
        }else{
            console.log('Program Not Found!')
        }
    }
Contoh lain
set validateEmail(email){
        if(email.includes('@') && email.includes('.')){
            this.email = email
        }else{
            console.log('Email Not Valid');
        }
    }


"GETTER"/"GET" Untuk mengambil data dari object --> get namaProperti() --> TIDAK BISA DALAM "IF" & DALAM VARIABLE --> Ada "return"
Contoh
get showPhoneNumber(){
        console.log(this.#phoneNumber.slice(0, this.#phoneNumber.length-6) + '*****')
    }


STATIC dalam CLASS JAVASCRIPT
 
// Case.
// Program Untuk Menyimpan Data Pendaftaran Student Purwadhika ke 1 Unit, 
// Validasi Data Inputnya
// name, username, email (Valid), password (Min: 6, Max: 10), address, 
// phoneNumber (Show Data: 08123335*****), 
// programSelected('JCWD', 'JCDM', 'JCDS', 'JCUI/UX', 'JCVD')

class StudentRegistration{
    name;
    username;
    email;
    #password;
    address;
    #phoneNumber;
    programSelected;

    constructor(name, username, address, phoneNumber){
        this.name = name;
        this.username = username; 
        this.address = address;
        this.#phoneNumber = phoneNumber; 
    }

    set validateEmail(email){
        if(email.includes('@') && email.includes('.')){
            this.email = email
        }else{
            console.log('Email Not Valid');
        }
    }
    set validatePassword(password){
        if(password.length < 6 || password.length > 10){
            console.log('Password Have Minimum Require')
        }else{
            this.#password = password
        }
    }
    set validateProgramSelected(program){
        const validPrograms = ['JCWD', 'JCDS', 'JCUI/UX', 'JCDM', 'JCVD']

        if(validPrograms.includes(program.toUpperCase())){
            this.programSelected = program.toUpperCase()
        }else{
            console.log('Program Not Found!')
        }
    }

    get showPhoneNumber(){
        console.log(this.#phoneNumber.slice(0, this.#phoneNumber.length-6) + '*****')
    }
}

const student1 = new StudentRegistration('Ryan', 'ryan01', 'Bogor', '0888')
student1.validateEmail = 'ryan@gmail.com'
student1.validatePassword = 'abc12345'
student1.validateProgramSelected = 'abcd'
student1.showPhoneNumber
console.log(student1)

Jika ada function / set / get pada class JavaScript TEMPATKAN DI LUAR { ... } DARI CONSTRACTOR seperti diatas